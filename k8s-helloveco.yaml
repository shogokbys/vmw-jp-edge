apiVersion: v1
kind: Namespace
metadata:
  name: hello-veco
  labels:
    app: metallb
---
apiVersion:  apps/v1
kind: Deployment
metadata:
  name: nginx-helloworld-deployment
  namespace: hello-veco
spec:
  selector:
    matchLabels:
      app: nginx-hello-veco
  replicas: 1 # tells deployment to run 1 pod matching the template
  template:
    metadata:
      labels:
        app: nginx-hello-veco
    spec:
      containers:
      - name: nginx-helloworld
        image: alanrenouf/docker-nginx-hello-keswick
        ports:
        - containerPort: 80
---
apiVersion: v1
kind: Service
metadata:
  name: ngnix-service
  namespace: hello-keswick
spec:
  selector:
    app: nginx-hello-veco
  type: NodePort
  ports:
  - protocol: TCP
    port: 80
    targetPort: 80
    nodePort: 30081
